# Build output
build/
build_vs/
cmake-build-*/

# Scratch output dir for wasm/build.sh. The bundle it produces is
# committed at website/app/uti.js; this is only where CI builds a second
# copy to compare against, and where a local rebuild can be staged.
build-wasm/

# IDE / tool local state
.vscode/
.idea/
.claude/
*.user

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake

# Compiled output
*.exe
*.out
*.o
*.obj
*.a
*.lib
*.dll
*.so
*.dylib
*.pdb

# MATLAB resources — reference only, not part of the library
Resources/

# Internal Claude Code working notes -- development narrative, not
# meant for public consumption. Kept locally only, never pushed.
CLAUDE.md

# Python bytecode from tools/
__pycache__/
*.pyc
